#!/usr/bin/env node

const args = process.argv.slice(2);

if (args.length == 1 && args[0] == '--version') {
  console.log("llm, version fake-llm");
  return;
}

console.log("unexpected arguments to fake-llm");
process.exit(1);
